import { UPDATE_HOUSE_VALUE } from "./types";

const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    // case UPDATE_HOUSE_VALUE:

    default:
      return state;
  }
};
